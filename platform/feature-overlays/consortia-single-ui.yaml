# platform/feature-overlays/consortia-single-ui.yaml — single combined consortium UI (§2.2b)
# Replaces the isConsortiaSingleUi boolean + RancherNamespace:184 branch.
schemaVersion: 1
name: consortia-single-ui

modules:
  mod-users-keycloak:
    extraEnvVars:
      - { name: SINGLE_TENANT_UX, value: "true" }
  mod-consortia-keycloak:
    extraEnvVars:
      - { name: SINGLE_TENANT_UX, value: "true" }

ui:
  consortiaSingleUx: true