# platform/feature-overlays/ecs-ccl.yaml — ECS cross-tenant loans / TLR (§2.2b)
# Replaces the EurekaNamespace:71 branch. The per-tenant isEcsBff UI effect stays
# pipeline-derived (consortia tenant && ecs-ccl active) — no config field.
schemaVersion: 1
name: ecs-ccl

modules:
  mod-inventory-storage:
    extraEnvVars:
      - { name: ECS_TLR_FEATURE_ENABLED, value: "true" }
  mod-circulation:
    extraEnvVars:
      - { name: ECS_TLR_FEATURE_ENABLED, value: "true" }