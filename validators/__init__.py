"""Epic D cross-level validators. Violation is the shared finding type; the
run_namespace/run_tree entry points are added in Task 8 once runner.py exists."""

from validators.core import Violation

__all__ = ["Violation"]
